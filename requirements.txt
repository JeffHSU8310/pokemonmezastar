streamlit>=1.30.0
pandas>=2.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
qrcode>=8.0.0
pillow>=10.0.0
fastapi>=0.109.0
uvicorn>=0.27.0
opencv-python-headless>=4.8.0
# fastapi.testclient (starlette TestClient) 需要 httpx，
# 否則 CI 的 `python -m unittest test_mezastar.py` 會直接 RuntimeError。
httpx>=0.27.0
